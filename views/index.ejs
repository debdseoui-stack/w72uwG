<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard | Home</title>

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #4a90e2, #50c9ce);
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      color: #333;
    }

    .dashboard {
      background-color: #fff;
      width: 90%;
      max-width: 500px;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
      text-align: center;
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    h1 {
      color: #4a90e2;
      margin-bottom: 20px;
    }

    a.home-link {
      display: inline-block;
      margin-bottom: 20px;
      text-decoration: none;
      color: #4a90e2;
      font-weight: 600;
      transition: color 0.3s ease;
    }

    a.home-link:hover {
      color: #3a78c2;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    input[type="submit"] {
      background-color: #4a90e2;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 12px;
      font-size: 16px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    input[type="submit"]:hover {
      background-color: #3a78c2;
      transform: translateY(-2px);
    }

    input[name="delete"] {
      background-color: #e74c3c;
    }

    input[name="delete"]:hover {
      background-color: #c0392b;
    }

    h4 {
      margin-top: 20px;
      color: #27ae60;
      font-weight: 600;
    }

    footer {
      margin-top: 25px;
      text-align: center;
      font-size: 13px;
      color: #eee;
    }

    @media (max-width: 480px) {
      .dashboard {
        width: 95%;
        padding: 25px;
      }
    }
  </style>
</head>

<body>
  <div class="dashboard">
    <h1>Admin Dashboard</h1>
    <a href="/backend" class="home-link">🏠 Home</a>

    <form method="POST" action="">
      <input type="submit" name="create" value="📁 Create Table">
      <input type="submit" name="delete" value="🗑️ Delete Table" onclick="return confirm('Are you sure to delete table?');">
      <input type="submit" name="getdata" value="📋 Get Data">
      <input type="submit" name="genurl" value="🔗 Generate URL">
    </form>

    <h4><%= message %></h4>
  </div>

  <footer>© 2025 Secure Admin Portal</footer>
</body>
</html>
